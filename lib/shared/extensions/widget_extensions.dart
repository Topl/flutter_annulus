import 'package:flutter/material.dart';
import 'package:hooks_riverpod/hooks_riverpod.dart';

abstract class HookConsumerScreenWidget extends HookConsumerWidget {
  const HookConsumerScreenWidget({
    Key? key,
  }) : super(key: key);
}
