import 'package:flutter/material.dart';

class LowerDetailSection extends StatelessWidget {
  const LowerDetailSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
